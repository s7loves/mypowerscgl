<?xml version="1.0" encoding="utf-8"?>
<sqlMapConfig
  xmlns="http://ibatis.apache.org/dataMapper"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

  <properties resource="./config/ibatis/properties.config"/>

  <settings>
    <setting useStatementNamespaces="false"/>
  </settings>

  <providers embedded="Ebada.Server.config.ibatis.providers.config"/>

  <!-- Database connection information -->
  <database>
    <provider name="${provider}"/>
    <dataSource name="Ebada" connectionString="${connectionString}"/>
  </database>
  <sqlMaps>
    <sqlMap embedded="${root}mModule.xml,${assembly}"/>
    <sqlMap embedded="${root}mOrg.xml,${assembly}"/>
    <sqlMap embedded="${root}mOrgLevel.xml,${assembly}"/>
    <sqlMap embedded="${root}Empolyees.xml,${assembly}"/>

  </sqlMaps>
</sqlMapConfig>